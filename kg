#!/bin/bash
# Wrapper que fuerza arquitectura arm64 para Python y scripts de knowledge-engine
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"
source "$SCRIPT_DIR/venv/bin/activate"
exec arch -arm64 python "$@"
